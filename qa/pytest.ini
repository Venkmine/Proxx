[pytest]
testpaths = proxy
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: marks tests as slow (run with verify proxy full)
    e2e: marks tests as end-to-end (requires FFmpeg)
