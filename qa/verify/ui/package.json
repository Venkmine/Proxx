{
  "name": "@awaire-proxy/verify-ui",
  "version": "1.0.0",
  "description": "Playwright UI tests for Awaire Proxy Verify system",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:browser": "playwright test --project=browser",
    "test:electron": "playwright test --project=electron",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report ../../../logs/playwright-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.3.0"
  }
}
